// TODO: Add your own header

#include <iostream>

// We need to #include rectarea_functions.h because the functions
// we are using in the main function are defined in a separate file.
#include "rectarea_functions.h"

int main(int argc, char const* argv[]) {
  // TODO: Declare an integer variable and name it input_length and
  // initialize it to the return value of PromptForInt(). Use the
  // prompt:
  // "Enter the length of the rectangle: "

  // TODO: Declare an integer variable and name it input_width. Intialize
  // input_width to a value entered by the computer user when they answer
  // the prompt:
  // "Enter the width of the rectangle: "

  // TODO: Declare an integer variable and name it area. Intialize
  // area to the return value of the function RectangleArea(). Use
  // the variables input_length and input_width as input to RectangleArea().

  // TODO: Print to the terminal the values of input_length, input_width, and
  // area such that they make a nicely formatted string such as:
  // 12 x 13 = 156
  // Hint: first print out input_length, then print out " x ", then print out
  // input_width, then print out " = ", then print out area, and finally
  // print out "\n"

  return 0;
}